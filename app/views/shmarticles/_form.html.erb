<%= form_for(@shmarticle) do |f| %>
  <% if @shmarticle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shmarticle.errors.count, "error") %> prohibited this shmarticle from being saved:</h2>

      <ul>
      <% @shmarticle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
