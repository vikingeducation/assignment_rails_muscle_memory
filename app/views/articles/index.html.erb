<h1>All Articles</h1>
<% @articles.each do |article| %>
  <p><%= link_to article.id, article_path(article) %> - <%= link_to article.title, article_path(article)%> | <%= link_to "Edit", edit_article_path(article.id) %> | <%= link_to "Delete", article_path(article.id), method: "delete" %>
<% end %>
