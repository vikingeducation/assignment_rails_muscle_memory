<ul>
  <% @articles.each do |article| %>
  <li>
      <%= link_to "#{article.title}", article_path(article)  %> <%= link_to "Edit this article", edit_article_path(article)%>
  </li>
  <% end %>
</ul>
