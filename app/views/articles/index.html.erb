<ul>
  <% @articles.each do |article| %>
    <li>
      <p><%= link_to article.title, article_path(article.id) %></p>
      <p><%= link_to article.id, article_path(article.id) %> </p>


    </li>

  <% end %>
</ul>